<p style="color: green"><%= notice %></p>

<%= render @project %>

<div>
  <%= link_to "Edit this project", edit_admin_project_path(@project) %> |
  <%= link_to "Back to projects", admin_projects_path %>
  <%= link_to "Destroy this project", [:admin, @project], data: { turbo_stream: false, turbo_method: :delete, turbo_confirm: "Are you sure?" } %>
</div>
